apiVersion: v1
kind: Service
metadata:
  name: db-auth-service
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: db-auth
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017