apiVersion: v1
kind: Service
metadata:
  name: db-auth-service
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: auth-db
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017